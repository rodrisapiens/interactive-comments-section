import { createContext } from "react";
import react,{ useState } from "react"
export const AppTimeContext=createContext();
export const CurrentUserContext=createContext();
export const likedContext=createContext();
